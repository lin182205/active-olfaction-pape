# 论文评审与修改说明

## 总体判断

选题具有明确的工程对象和可检验问题：通过可动双触须改变气体传感器采样位置，在间歇羽流和慢响应传感器条件下提高有效信息获取。现稿已经具备硬件、仿真、观测、算法和评估链路，适合作为方法型论文雏形；目前最大的缺口不是算法描述不够长，而是核心主张与实验因果链尚未对齐，且正式结果尚未产生。

## 优先级最高的问题

1. **核心问题曾被移动找源掩盖。** 原稿把移动机器人到源作为主问题，但项目最有辨识度的贡献是主动控制采样位置。移动成功率同时受底盘策略、距离奖励、时序网络和触须动作影响，不能直接证明触须有效。修订稿已改为两级证据链：固定基座先验证信息增益，移动任务再验证下游转化。

2. **主基线必须只改变触须控制方式。** “固定触须 DQN vs 主动触须 PPO”同时改变算法和触须能力，归因不成立。主比较应统一 PPO、时序编码、底盘动作、训练预算和评估场景，只把触须替换为多个固定角度、周期扫描、随机扫描或主动策略。DQN 可保留为补充算法对照。

3. **距离塑形会削弱嗅觉驱动结论。** 当前移动奖励使用真实到源距离差。该量不输入策略，所以部署接口没有泄漏；但训练仍接受 oracle 监督。论文必须增加 `progress_reward_scale=0` 对照，并把默认距离塑形结果表述为工程训练条件。

4. **正式实验是当前硬缺口。** 在多随机种子、固定测试场景和真机对照完成前，论文只能主张“提出方法和验证协议”，不能主张“优于固定传感器”或“实现可靠找源”。修订稿已把结论边界写清楚。

## 已直接修正的问题

- 标题、摘要、引言和结论统一到“主动采样优先、移动找源次级验证”的研究主线。
- 增加相关工作与研究空白，补入机器人嗅觉综述、循环强化学习羽流追踪、嗅觉 POMDP 和 GADEN 等关键文献。
- 修正实际环境触须长度：`0.15 m`，不是原稿中的 `0.3 m`。
- 区分预处理尺度：仿真 `scale=0.35`、硬件阶段暂定 `scale=1500 ADC`，`scale=200` 只是通用配置回退值。
- 明确当前只完成双触须—STM32—上位机最小闭环；移动底盘、航向估计和统一时钟尚待真机集成。
- 补充 MQ-3 对酒精/乙醇类刺激的适用边界，避免把原型结果直接泛化到所有危险气体。
- 修正移动环境单步执行顺序的文字描述，使其与代码一致。
- 重写实验章：加入 H1/H2、固定基座主实验、共同随机数、训练 seed 统计单位、角位移代价和真机随机区组设计。

## 后续最值得投入的工作

1. 先完成固定基座仿真对照，检查 active 是否优于 best fixed、mean fixed、periodic 和 random-neighbor；若无增益，优先调整采样几何、停留时间或羽流间歇性，不急于扩大网络。
2. 用同一实验顺序设计完成真机采样模式对比，保存原始 ADC、时间戳、扇区、环境条件和预处理配置。
3. 冻结环境版本后再做至少 5 个训练 seed 的移动实验；主结果按训练 seed 汇总，不把同一模型的 episode 数当独立样本量。
4. 补齐图表时优先放证据图：羽流间歇性诊断、固定基座主结果、移动成功率/失嗅指标、奖励投机诊断和典型轨迹。网络结构图可保留，但不应压过实验证据。
5. 最终投稿前根据目标期刊压缩第3章通用 PPO 教材性推导，把篇幅让给实验设置、效应量、失败案例和真机可复现细节。

## 当前建议的论文结论强度

在数据补齐前，可写“构建并实现了双触须主动采样与移动搜索的统一方法和验证框架”；不可写“证明主动触须优于固定传感器”。固定基座和移动两级结果均支持后，才可进一步写“主动采样提高了信息获取，并在移动找源中转化为性能收益”。
